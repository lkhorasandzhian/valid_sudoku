name: valid_sudoku
description: Author - Levon Khorasandzhian (@lkhorasandzhian)

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Helpful library for data providing.
  provider: ^6.0.2

  # Essential library for window size control.
  window_manager: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
